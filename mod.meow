 -- Runs when the mod is loaded
on_load = fun {
    execute_mod_code_file("sprites/sprites.meow")
    execute_mod_code_file("items/items.meow")
    execute_mod_code_file("perks/perks.meow")
    execute_mod_code_file("supervisors/supervisors.meow")
}
-- Runs when the mod is unloaded
-- Please clean up any dynamic structures you created here
-- as to not cause memory leaks
on_unload = fun {

}
